/*03) Crie uma função que recebe dois parâmetros, base e expoente, e retorne a base elevada ao expoente*/

function potenciacao(base:number, expoente:number) {
    console.log(`${base} elevado a ${expoente} = ${base ** expoente}`)
}

potenciacao(2, 4)