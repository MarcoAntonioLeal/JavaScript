/*06) Crie um programa que calcule e exiba o perímetro de um círculo, solicitando o raio ao usuário. C = 2 * π * r*/
function perimetroCirculo(raio) {
    console.log((2 * raio * Math.PI).toFixed(2));
}
perimetroCirculo(5);
